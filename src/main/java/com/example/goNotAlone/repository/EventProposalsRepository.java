package com.example.goNotAlone.repository;

import com.example.goNotAlone.model.EventProposals;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EventProposalsRepository extends JpaRepository<EventProposals,Long> {
}
